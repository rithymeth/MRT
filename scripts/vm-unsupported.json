[
  "example: coroutines.mrt",
  "example: destructuring.mrt",
  "example: errors.mrt",
  "example: expressions.mrt",
  "example: generators.mrt",
  "example: json.mrt",
  "example: lazy.mrt",
  "example: matching.mrt",
  "example: modules.mrt",
  "example: structs.mrt",
  "modules: a bare specifier is rejected",
  "modules: a cycle three modules deep is detected and named in order",
  "modules: a missing module reports the specifier",
  "modules: a module importing itself is a cycle",
  "modules: a module is evaluated once no matter how many importers",
  "modules: a module with a syntax error reports it against the specifier",
  "modules: a nested module resolves its own imports relative to itself",
  "modules: a struct declared in one module is usable from another",
  "modules: an error in a module's top level propagates with its own line",
  "modules: circular imports are detected and named",
  "modules: destructuring an imported object across module boundaries",
  "modules: imported closures keep sharing their module state",
  "modules: importing a directory reports a missing module",
  "modules: importing a name a module does not export",
  "modules: module scope is isolated from the importer but sees globals",
  "modules: module top-level code runs before the entry main()",
  "modules: modules combine with defaults, rest, errors and interpolation",
  "modules: named imports, aliasing, and nested module paths",
  "modules: namespace imports bind one object of every export",
  "modules: private names are not importable",
  "modules: re-exporting a name the source module lacks",
  "modules: re-exports forward another module without binding locally",
  "modules: two specifiers reaching the same file share one evaluation",
  "regression: a generator function is lazy and yields in order",
  "regression: a generator keeps its own scope across suspensions",
  "regression: a generator method on a struct sees this",
  "regression: a generator resumes where an earlier consumer stopped",
  "regression: a rethrown error from a guard-matched clause reaches the outer try",
  "regression: a struct pattern with the wrong field count is an error",
  "regression: a struct with iter() is iterable everywhere an iterable is",
  "regression: a yield with no sender sees null, and assigns through any target",
  "regression: abandoning a generator mid-loop leaves the caller intact",
  "regression: abandoning many generators does not corrupt the interpreter",
  "regression: an abandoned generator runs no finally, a finished one does",
  "regression: an endless generator is consumed only as far as asked",
  "regression: an error raised inside a catch guard propagates",
  "regression: an exhausted generator is an error to iterate but not to step",
  "regression: an iter() that returns the struct itself is reported, not looped",
  "regression: an unmatched guard lets the error keep propagating, finally still runs",
  "regression: an unmatched match expression is an error, and match is still a statement",
  "regression: arity errors describe the accepted range",
  "regression: array and object destructuring assignment, including swap",
  "regression: array destructuring in var, with rest and defaults",
  "regression: array patterns match length exactly unless a rest is given",
  "regression: boolean and numeric object keys are distinct, as == says they are",
  "regression: built-in runtime errors are catchable with message and line",
  "regression: case guards, tried in order, first match wins",
  "regression: catch guards select by kind, unguarded clause is the fallback",
  "regression: caught error object shape and key order",
  "regression: closures capture a generator per call, independently",
  "regression: destructured for-in still binds fresh per iteration",
  "regression: destructuring assignment needs existing variables and the right shape",
  "regression: destructuring assignment nests, renames, defaults and rests",
  "regression: destructuring assignment reaches outer scopes, not new ones",
  "regression: destructuring failures are strict and classified",
  "regression: destructuring in for-in and catch",
  "regression: error kind classification across the language",
  "regression: error stack trace is innermost-first across frames",
  "regression: errors and throws propagate out of a generator with a frame",
  "regression: field assignment is restricted to declared fields",
  "regression: field defaults, including ones referring to earlier fields",
  "regression: finally runs on normal exit, on catch, and on return",
  "regression: from and as are contextual keywords, usable as ordinary names",
  "regression: generators are single use",
  "regression: generators chain into streaming pipelines",
  "regression: indexing errors carry the line they were written on",
  "regression: iter() may return any iterable, and a struct without it iterates fields",
  "regression: lazy map and filter over an endless generator",
  "regression: lazy map is only as lazy as it is asked to be",
  "regression: literal patterns, including negatives, booleans and null",
  "regression: map and filter stay eager on arrays, and still reject other types",
  "regression: match bindings are scoped to their case",
  "regression: match expressions bind, guard and default",
  "regression: match expressions nest and compose with calls and interpolation",
  "regression: methods see `this`, and stay bound when pulled off the instance",
  "regression: modulo takes the sign of the dividend, as in C and JavaScript",
  "regression: nested patterns combining arrays, objects and structs",
  "regression: nested patterns mixing arrays and objects",
  "regression: nested try, rethrow from a catch block",
  "regression: next/send drive a generator by hand and report done",
  "regression: next/send reject non-generators and tolerate exhaustion",
  "regression: no matching case without a default is an error",
  "regression: object destructuring in var, with renaming, defaults and rest",
  "regression: object patterns are partial and can nest",
  "regression: resuming a generator from inside itself is an error",
  "regression: return ends a generator early",
  "regression: spread in calls, array literals and print",
  "regression: spreading a non-array is a type error",
  "regression: struct construction, fields, stringify and type()",
  "regression: struct constructor arity errors",
  "regression: struct equality is structural and per-struct",
  "regression: struct patterns match the exact struct and bind fields in order",
  "regression: structs answer keys/values/has/get/len and object destructuring",
  "regression: structs are hoisted, nest, and work with the collection pipeline",
  "regression: take and toArray work on every iterable, and reject the rest",
  "regression: take pulls exactly as many items as asked",
  "regression: the higher-order builtins accept anything iterable",
  "regression: the value sent on the first step is discarded",
  "regression: throw and catch a plain value",
  "regression: try/finally with no catch propagates the error",
  "regression: uncaught throw halts the program and reports the value",
  "regression: yield inside if, for, for-in, try and match",
  "regression: yield* delegates to generators, arrays, strings and objects",
  "regression: yield* forwards sent values into the delegate"
]
