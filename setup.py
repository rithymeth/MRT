"""Packaging entry point kept only for tools that don't yet read
pyproject.toml directly. All actual metadata lives in pyproject.toml --
see [project] there."""

from setuptools import setup

setup()
